# I&FP 백로그 (배포 후 결정)

Generated by /plan-ceo-review on 2026-05-03
Last updated: 2026-05-03

---

## 배포 후 — 성능 관찰 후 결정

### [ ] ChromaDB 커넥션 캐싱
- **파일**: `src/retrievers/chroma_retriever.py`
- **현재**: `Chroma(...)` 객체를 요청마다 새로 생성 — 커넥션 재사용 없음
- **수정 방향**: module-level singleton 또는 간단한 LRU 캐시
- **트리거**: 응답 속도가 실사용자에게 문제가 된다고 피드백받을 때
- **예상 공수**: CC ~15분 / 사람 ~30분

---

## 배포 후 — 코드 정리

### [ ] Dead code 제거
- **파일**: `src/rfp_graph.py:100-133`
- **현재**: `build_rfp_analyze_graph()`, `build_rfp_match_graph()` — 어디서도 호출 안 됨
- **수정 방향**: 두 함수 삭제, 실제 진입점 `run_analyze_with_events()` / `run_match_with_events()` 유지
- **트리거**: 배포 후 코드베이스 안정되면 정리 타임에
- **예상 공수**: CC ~10분 / 사람 ~10분

---

## 배포 후 — 실 사용자 피드백 수집 후 결정

### [ ] 테스트 작성
- **현재**: `tests/` 디렉토리 있지만 실질적 테스트 없음
- **수정 방향**: 실 사용자 피드백을 보고 회귀 위험이 높은 경로부터 테스트 작성
  - RAG retrieval 결과 일관성 검증
  - Review pass/fail 정확도
  - SSE 스트리밍 종료 조건
- **트리거**: 제안팀 사용자가 "이상하게 작동함" 피드백을 2회 이상 주면
- **예상 공수**: CC ~2시간 / 사람 ~반나절

---

## 언제든 전환 가능 (환경변수만 변경)

### [ ] Claude API 실제 전환
- **현재**: `GENERATOR_USE_CLAUDE` 환경변수 추가 후 → `generator.py`가 Claude API 사용
- **수정**: `.env`에 `GENERATOR_USE_CLAUDE=true` 추가
- **트리거**: EXAONE 생성 제안서 품질이 "실무에 부족하다"는 피드백받을 때
- **비용 예상**: Claude Sonnet 기준 RFP 1건 분석 ≈ 약 $0.05–0.15 (입력 길이 따라)
- **예상 공수**: CC ~0분 / 사람 ~2분 (.env 수정만)

---

## 참고: 이번 배포에 포함된 항목

배포에 포함된 수정 목록은 `~/.gstack/projects/IFP/ceo-plans/2026-05-03-ifp.md` 참고.
